fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
aiosqlite==0.20.0
chromadb==0.5.5
httpx==0.27.2
python-multipart==0.0.9
beautifulsoup4==4.12.3
html2text==2024.2.26
python-docx==1.1.2
openpyxl==3.1.5
# pypdf is the maintained successor of PyPDF2 (same PdfReader API, plans/013)
pypdf==5.1.0
# Renders page one of a PDF into the card's cover (core/pdf_thumb.py). pypdf
# reads text and cannot rasterize. Permissively licensed wheels that bundle
# PDFium, so unlike pdf2image there is no poppler to install in the image, and
# unlike PyMuPDF it stays small.
pypdfium2>=4.30.0
pydantic==2.9.2
pydantic-settings==2.5.2
# yt-dlp breaks whenever YouTube changes its player (every few weeks), so a
# hard pin guarantees staleness. Floor-pin to a recent release and let the
# Docker entrypoint self-update to the newest nightly on container start.
yt-dlp>=2025.1.0
# Instagram photo posts at full display resolution (uncropped) through the
# user's cookie jar — tier 1 of the IG photo ladder (ADR-020). yt-dlp can't
# pull photo posts at all; gallery-dl is purpose-built for them + carousels.
gallery-dl>=1.30.0
readability-lxml==0.8.1
lxml==5.3.0
pillow==10.4.0
websockets==12.0
apscheduler==3.10.4
# Local speech-to-text for audio memos. Pulls ctranslate2 + av (bundled
# ffmpeg) + tokenizers; CPU (int8) or CUDA (float16). Multilingual.
faster-whisper>=1.0.0
# Reads artist/title tags from uploaded music files (ID3 / MP4 / Vorbis). Tiny,
# pure-Python; best-effort — ingest works without it (no artist shown). (ADR-010)
mutagen>=1.47.0
# Headless Chromium driver for the local link scraper (backend/core/headless.py),
# the self-hosted replacement for Microlink. The browser binary itself is pulled
# in the Dockerfile via `patchright install` (patchright is a patched Playwright
# fork that removes CDP fingerprint markers Cloudflare Turnstile checks — same
# API, drop-in replacement, but passes Turnstile without a CAPTCHA service).
playwright>=1.49.0
patchright>=1.49.0

# Dev dependencies
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# Mesh (ADR-024). Two small pure-python additions, both for the pairing step:
#   mnemonic - the standard BIP39 wordlist. Deliberately NOT hand-rolled: a
#              wrong wordlist would be a silent correctness bug in the one
#              value the user writes on paper and cannot re-derive.
#   qrcode   - SVG output only, so no Pillow and no binary dependency.
mnemonic>=0.20
qrcode>=7.4
#   cryptography - AES-256-GCM for the sync channel. Replaces a hand-rolled
#              keystream that was acceptable on a LAN and is not once the
#              laptop syncs from a cafe.
cryptography>=42.0
#   zeroconf     - mDNS, so two machines find each other with no address typed.
#                  Optional at runtime: Docker blocks multicast and Mesh still
#                  works by address there.
zeroconf>=0.132
